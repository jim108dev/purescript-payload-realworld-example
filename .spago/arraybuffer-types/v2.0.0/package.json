{
  "private": true,
  "scripts": {
    "clean": "rimraf output",
    "build": "psa --censor-lib --strict 'src/**/*.purs'"
  },
  "devDependencies": {
    "purescript-psa": "^0.5.0",
    "purescript": "^0.11.1",
    "rimraf": "^2.6.1"
  }
}
